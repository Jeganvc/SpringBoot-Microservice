server:
  port: 9626
  
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jeganvc/cloud-config.git
          clone-on-start: true
  